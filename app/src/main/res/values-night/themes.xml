<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartSpendMax" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorSelected</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="android:windowBackground">@color/colorWhite</item>
        <item name="colorOnPrimary">@color/colorBlack</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorPrimaryMedium</item>
        <item name="colorOnSecondary">@color/colorWhite</item>

        <!-- Default text color -->
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <!-- Spinner Popup Background -->
        <item name="android:popupBackground">@color/colorWhite</item>

        <!-- Navigation bar color -->
        <item name="android:navigationBarColor">@color/colorBlack</item>

        <item name="android:fontFamily">@font/sourcesans400</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/colorBlack</item>
    </style>


</resources>